/*
 * Copyright The OpenTelemetry Authors 
 * SPDX-License-Identifier: Apache-2.0
 */

// DO **NOT** EDIT. This file is automatically generated.

import Foundation 

extension SemanticConventions {
  public enum Vcs: String {

    /**
     The ID of the change (pull request/merge request/changelist) if applicable. This is usually a unique (within repository) identifier generated by the VCS system.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.changeId.rawValue] = "123"
      ```

     - Requires: Value type should be `String`
    */
    case changeId = "vcs.change.id"

    /**
     The state of the change (pull request/merge request/changelist).

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.changeState.rawValue] = .open
      attributes[SemanticConventions.Vcs.changeState.rawValue] = .closed
      attributes[SemanticConventions.Vcs.changeState.rawValue] = .merged
      ```

     - Requires: Value should be one of ``ChangeStateValues`` (of type `String`)
    */
    case changeState = "vcs.change.state"

    /**
     The human readable title of the change (pull request/merge request/changelist). This title is often a brief summary of the change and may get merged in to a ref as the commit summary.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.changeTitle.rawValue] = "Fixes broken thing"
      attributes[SemanticConventions.Vcs.changeTitle.rawValue] = "feat: add my new feature"
      attributes[SemanticConventions.Vcs.changeTitle.rawValue] = "[chore] update dependency"
      ```

     - Requires: Value type should be `String`
    */
    case changeTitle = "vcs.change.title"

    /**
     The type of line change being measured on a branch or change.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.lineChangeType.rawValue] = .added
      attributes[SemanticConventions.Vcs.lineChangeType.rawValue] = .removed
      ```

     - Requires: Value should be one of ``LineChangeTypeValues`` (of type `String`)
    */
    case lineChangeType = "vcs.line_change.type"

    /**
     The group owner within the version control system.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.ownerName.rawValue] = "my-org"
      attributes[SemanticConventions.Vcs.ownerName.rawValue] = "myteam"
      attributes[SemanticConventions.Vcs.ownerName.rawValue] = "business-unit"
      ```

     - Requires: Value type should be `String`
    */
    case ownerName = "vcs.owner.name"

    /**
     The name of the version control system provider.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.providerName.rawValue] = .github
      attributes[SemanticConventions.Vcs.providerName.rawValue] = .gitlab
      attributes[SemanticConventions.Vcs.providerName.rawValue] = .gitea
      attributes[SemanticConventions.Vcs.providerName.rawValue] = .bitbucket
      ```

     - Requires: Value should be one of ``ProviderNameValues`` (of type `String`)
    */
    case providerName = "vcs.provider.name"

    /**
     The name of the [reference](https://git-scm.com/docs/gitglossary#def_ref) such as **branch** or **tag** in the repository.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refBaseName.rawValue] = "my-feature-branch"
      attributes[SemanticConventions.Vcs.refBaseName.rawValue] = "tag-1-test"
      ```

     - Note: `base` refers to the starting point of a change. For example, `main`
       would be the base reference of type branch if you've created a new
       reference of type branch from it and created new commits.

     - Requires: Value type should be `String`
    */
    case refBaseName = "vcs.ref.base.name"

    /**
     The revision, literally [revised version](https://www.merriam-webster.com/dictionary/revision), The revision most often refers to a commit object in Git, or a revision number in SVN.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refBaseRevision.rawValue] = "9d59409acf479dfa0df1aa568182e43e43df8bbe28d60fcf2bc52e30068802cc"
      attributes[SemanticConventions.Vcs.refBaseRevision.rawValue] = "main"
      attributes[SemanticConventions.Vcs.refBaseRevision.rawValue] = "123"
      attributes[SemanticConventions.Vcs.refBaseRevision.rawValue] = "HEAD"
      ```

     - Note: `base` refers to the starting point of a change. For example, `main`
       would be the base reference of type branch if you've created a new
       reference of type branch from it and created new commits. The
       revision can be a full [hash value (see
       glossary)](https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.186-5.pdf),
       of the recorded change to a ref within a repository pointing to a
       commit [commit](https://git-scm.com/docs/git-commit) object. It does
       not necessarily have to be a hash; it can simply define a [revision
       number](https://svnbook.red-bean.com/en/1.7/svn.tour.revs.specifiers.html)
       which is an integer that is monotonically increasing. In cases where
       it is identical to the `ref.base.name`, it SHOULD still be included.
       It is up to the implementer to decide which value to set as the
       revision based on the VCS system and situational context.

     - Requires: Value type should be `String`
    */
    case refBaseRevision = "vcs.ref.base.revision"

    /**
     The type of the [reference](https://git-scm.com/docs/gitglossary#def_ref) in the repository.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refBaseType.rawValue] = .branch
      attributes[SemanticConventions.Vcs.refBaseType.rawValue] = .tag
      ```

     - Note: `base` refers to the starting point of a change. For example, `main`
       would be the base reference of type branch if you've created a new
       reference of type branch from it and created new commits.

     - Requires: Value should be one of ``RefBaseTypeValues`` (of type `String`)
    */
    case refBaseType = "vcs.ref.base.type"

    /**
     The name of the [reference](https://git-scm.com/docs/gitglossary#def_ref) such as **branch** or **tag** in the repository.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refHeadName.rawValue] = "my-feature-branch"
      attributes[SemanticConventions.Vcs.refHeadName.rawValue] = "tag-1-test"
      ```

     - Note: `head` refers to where you are right now; the current reference at a
       given time.

     - Requires: Value type should be `String`
    */
    case refHeadName = "vcs.ref.head.name"

    /**
     The revision, literally [revised version](https://www.merriam-webster.com/dictionary/revision), The revision most often refers to a commit object in Git, or a revision number in SVN.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refHeadRevision.rawValue] = "9d59409acf479dfa0df1aa568182e43e43df8bbe28d60fcf2bc52e30068802cc"
      attributes[SemanticConventions.Vcs.refHeadRevision.rawValue] = "main"
      attributes[SemanticConventions.Vcs.refHeadRevision.rawValue] = "123"
      attributes[SemanticConventions.Vcs.refHeadRevision.rawValue] = "HEAD"
      ```

     - Note: `head` refers to where you are right now; the current reference at a
       given time.The revision can be a full [hash value (see
       glossary)](https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.186-5.pdf),
       of the recorded change to a ref within a repository pointing to a
       commit [commit](https://git-scm.com/docs/git-commit) object. It does
       not necessarily have to be a hash; it can simply define a [revision
       number](https://svnbook.red-bean.com/en/1.7/svn.tour.revs.specifiers.html)
       which is an integer that is monotonically increasing. In cases where
       it is identical to the `ref.head.name`, it SHOULD still be included.
       It is up to the implementer to decide which value to set as the
       revision based on the VCS system and situational context.

     - Requires: Value type should be `String`
    */
    case refHeadRevision = "vcs.ref.head.revision"

    /**
     The type of the [reference](https://git-scm.com/docs/gitglossary#def_ref) in the repository.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refHeadType.rawValue] = .branch
      attributes[SemanticConventions.Vcs.refHeadType.rawValue] = .tag
      ```

     - Note: `head` refers to where you are right now; the current reference at a
       given time.

     - Requires: Value should be one of ``RefHeadTypeValues`` (of type `String`)
    */
    case refHeadType = "vcs.ref.head.type"

    /**
     The type of the [reference](https://git-scm.com/docs/gitglossary#def_ref) in the repository.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.refType.rawValue] = .branch
      attributes[SemanticConventions.Vcs.refType.rawValue] = .tag
      ```

     - Requires: Value should be one of ``RefTypeValues`` (of type `String`)
    */
    case refType = "vcs.ref.type"

    /**
     The human readable name of the repository. It SHOULD NOT include any additional identifier like Group/SubGroup in GitLab or organization in GitHub.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.repositoryName.rawValue] = "semantic-conventions"
      attributes[SemanticConventions.Vcs.repositoryName.rawValue] = "my-cool-repo"
      ```

     - Note: Due to it only being the name, it can clash with forks of the same
       repository if collecting telemetry across multiple orgs or groups in
       the same backends.

     - Requires: Value type should be `String`
    */
    case repositoryName = "vcs.repository.name"

    /**
     The [canonical URL](https://support.google.com/webmasters/answer/10347851) of the repository providing the complete HTTP(S) address in order to locate and identify the repository through a browser.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.repositoryUrlFull.rawValue] = "https://github.com/opentelemetry/open-telemetry-collector-contrib"
      attributes[SemanticConventions.Vcs.repositoryUrlFull.rawValue] = "https://gitlab.com/my-org/my-project/my-projects-project/repo"
      ```

     - Note: In Git Version Control Systems, the canonical URL SHOULD NOT include
       the `.git` extension.

     - Requires: Value type should be `String`
    */
    case repositoryUrlFull = "vcs.repository.url.full"

    /**
     The type of revision comparison.

      - Examples:
      ```
      attributes[SemanticConventions.Vcs.revisionDeltaDirection.rawValue] = .ahead
      attributes[SemanticConventions.Vcs.revisionDeltaDirection.rawValue] = .behind
      ```

     - Requires: Value should be one of ``RevisionDeltaDirectionValues`` (of type `String`)
    */
    case revisionDeltaDirection = "vcs.revision_delta.direction"

    /** 
      The state of the change (pull request/merge request/changelist).
    */
    public struct ChangeStateValues: CustomStringConvertible, Sendable {
      
      /// Open means the change is currently active and under review. It hasn't been merged into the target branch yet, and it's still possible to make changes or add comments.
      public static let open = ChangeStateValues("open") 
      
      /// WIP (work-in-progress, draft) means the change is still in progress and not yet ready for a full review. It might still undergo significant changes.
      public static let wip = ChangeStateValues("wip") 
      
      /// Closed means the merge request has been closed without merging. This can happen for various reasons, such as the changes being deemed unnecessary, the issue being resolved in another way, or the author deciding to withdraw the request.
      public static let closed = ChangeStateValues("closed") 
      
      /// Merged indicates that the change has been successfully integrated into the target codebase.
      public static let merged = ChangeStateValues("merged") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }

    /** 
      The type of line change being measured on a branch or change.
    */
    public struct LineChangeTypeValues: CustomStringConvertible, Sendable {
      
      /// How many lines were added.
      public static let added = LineChangeTypeValues("added") 
      
      /// How many lines were removed.
      public static let removed = LineChangeTypeValues("removed") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }

    /** 
      The name of the version control system provider.
    */
    public struct ProviderNameValues: CustomStringConvertible, Sendable {
      
      /// [GitHub](https://github.com)
      public static let github = ProviderNameValues("github") 
      
      /// [GitLab](https://gitlab.com)
      public static let gitlab = ProviderNameValues("gitlab") 
      
      /// [Gitea](https://gitea.io)
      public static let gitea = ProviderNameValues("gitea") 
      
      /// [Bitbucket](https://bitbucket.org)
      public static let bitbucket = ProviderNameValues("bitbucket") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }

    /** 
      The type of the [reference](https://git-scm.com/docs/gitglossary#def_ref) in the repository.
    */
    public struct RefBaseTypeValues: CustomStringConvertible, Sendable {
      
      /// [branch](https://git-scm.com/docs/gitglossary#Documentation/gitglossary.txt-aiddefbranchabranch)
      public static let branch = RefBaseTypeValues("branch") 
      
      /// [tag](https://git-scm.com/docs/gitglossary#Documentation/gitglossary.txt-aiddeftagatag)
      public static let tag = RefBaseTypeValues("tag") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }

    /** 
      The type of the [reference](https://git-scm.com/docs/gitglossary#def_ref) in the repository.
    */
    public struct RefHeadTypeValues: CustomStringConvertible, Sendable {
      
      /// [branch](https://git-scm.com/docs/gitglossary#Documentation/gitglossary.txt-aiddefbranchabranch)
      public static let branch = RefHeadTypeValues("branch") 
      
      /// [tag](https://git-scm.com/docs/gitglossary#Documentation/gitglossary.txt-aiddeftagatag)
      public static let tag = RefHeadTypeValues("tag") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }

    /** 
      The type of the [reference](https://git-scm.com/docs/gitglossary#def_ref) in the repository.
    */
    public struct RefTypeValues: CustomStringConvertible, Sendable {
      
      /// [branch](https://git-scm.com/docs/gitglossary#Documentation/gitglossary.txt-aiddefbranchabranch)
      public static let branch = RefTypeValues("branch") 
      
      /// [tag](https://git-scm.com/docs/gitglossary#Documentation/gitglossary.txt-aiddeftagatag)
      public static let tag = RefTypeValues("tag") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }

    /** 
      The type of revision comparison.
    */
    public struct RevisionDeltaDirectionValues: CustomStringConvertible, Sendable {
      
      /// How many revisions the change is behind the target ref.
      public static let behind = RevisionDeltaDirectionValues("behind") 
      
      /// How many revisions the change is ahead of the target ref.
      public static let ahead = RevisionDeltaDirectionValues("ahead") 

      internal let value: String 

      public init(_ customValue: String) {
        self.value = customValue
      }

      public var description: String { 
        return value
      }
    }
  }
}